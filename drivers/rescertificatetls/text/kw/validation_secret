The name of the secret object name hosting the certificate authority files for
certificate_secret validation.

The secret must have the `trusted_ca` and `verify_certificate_hash` keys set.

This setting makes the validation data served to envoy via the secret
discovery service, which allows certificates live rotation.
