package kwoption

const (
	ScheduleCompliance = "comp_schedule"
	ScheduleInfo       = "info_schedule"
	ScheduleMonitor    = "monitor_schedule"
	ScheduleStatus     = "status_schedule"

	// ScheduleInfoAlias is the name ScheduleInfo had when the refreshed
	// key-values were pushed to the collector by the instance node.
	ScheduleInfoAlias = "resinfo_schedule"
)
